apiVersion: v1
kind: PersistantVolume
metadata:
  name: grafana-pv
spec:
  capacity:
    storage: 5Gi
  accessmodes:
     -ReadWriteOnce
  persistantvolumeReclaimPolicy: Retain
  hostpath:
    path: mnt/data/grafana
